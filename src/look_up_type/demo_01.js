// look up type in TypeScript
'use strict';
function updateCategory(id, cate) { }
var tutProperty = 'id';
var someString = tutProperty;
// tutProperty = someString
function getProperty(obj, key) {
    return obj[key];
}
var tut = {
    title: 'machine learning',
    lesson: 12
};
var tutTitle = getProperty(tut, 'title');
console.log(tutTitle);
document.addEventListener('click', function (e) {
});
function handleEvent(eventName, callback) {
}
handleEvent('click', function (e) { });
handleEvent('keypress', function (e) { });
