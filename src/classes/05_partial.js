var PartialDemoOne;
(function (PartialDemoOne) {
    var updateTut = function (id, tut) { };
    updateTut(1, {
        title: 'machine learning'
    });
    // Req
    var addTensor = function (tensor) {
    };
    addTensor({
        size: 12,
        dim: 12
    });
})(PartialDemoOne || (PartialDemoOne = {}));
